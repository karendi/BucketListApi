machine:
  python:
    version: 3.6.0
dependencies:
  override:
    - pip install -r requirements.txt
test:
  override:
    - nosetests -v --cover-package=app  --with-coverage tests/
  post:
      - coveralls